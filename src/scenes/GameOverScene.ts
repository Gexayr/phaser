// @ts-ignore
import Phaser from "phaser";

export default class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }

    create() {
        // Here you can handle game over logic and show game over screen
    }
}